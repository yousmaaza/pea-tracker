# ═══════════════════════════════════════════════════════════════
# PEA Tracker - Configuration Environnement
# ═══════════════════════════════════════════════════════════════
#
# Copier ce fichier vers config/.env et remplir les valeurs
# IMPORTANT: config/.env est dans .gitignore - ne jamais commiter les secrets!

# ───────────────────────────────────────────────────────────────
# Claude API (REQUIS)
# ───────────────────────────────────────────────────────────────
# Clé API Anthropic pour claude-code
# Obtenir sur: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ───────────────────────────────────────────────────────────────
# MCP Yahoo Finance (REQUIS)
# ───────────────────────────────────────────────────────────────
# Chemin absolu vers le serveur MCP Yahoo Finance local
# Le serveur doit contenir server.py
YAHOO_FINANCE_MCP_PATH=/Users/yousrids/Documents/projets/yahoo-finance-mcp

# ───────────────────────────────────────────────────────────────
# Notifications (REQUIS)
# ───────────────────────────────────────────────────────────────
# Email pour recevoir les alertes d'erreur
EMAIL_RECIPIENT=votre@email.com

# ───────────────────────────────────────────────────────────────
# Configuration Logs (OPTIONNEL)
# ───────────────────────────────────────────────────────────────
# Niveau de log: debug, info, error
# Par défaut: info
LOG_LEVEL=info

# ───────────────────────────────────────────────────────────────
# Configuration MCP (OPTIONNEL)
# ───────────────────────────────────────────────────────────────
# Timeout pour health check du serveur MCP (secondes)
# Par défaut: 30
MCP_HEALTH_CHECK_TIMEOUT=30

# ───────────────────────────────────────────────────────────────
# Configuration Google Drive (OPTIONNEL)
# ───────────────────────────────────────────────────────────────
# Ces chemins sont utilisés par l'agent Market Watcher
# Par défaut: PEA-Tracker/
# DRIVE_FOLDER_WATCHLIST="PEA-Tracker/Data"
# DRIVE_FOLDER_SIGNALS_LOG="PEA-Tracker/Reports/Signaux"

# ───────────────────────────────────────────────────────────────
# Configuration Agent (OPTIONNEL)
# ───────────────────────────────────────────────────────────────
# Paramètres de l'agent Market Watcher
# MIN_CONFIDENCE_SCORE=60
# MAX_DAILY_ALERTS=10

# ═══════════════════════════════════════════════════════════════
# FIN DE LA CONFIGURATION
# ═══════════════════════════════════════════════════════════════
#
# Après avoir configuré ce fichier:
# 1. Copier vers config/.env : cp config/.env.template config/.env
# 2. Éditer config/.env avec vos vraies valeurs
# 3. Vérifier avec: ./scripts/utils/check-prerequisites.sh
